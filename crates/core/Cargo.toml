[package]
name = "ngv-opx-core"
version = "0.1.2"
edition.workspace = true
license.workspace = true
description = "Pure-Rust option pricing core: Black-76 + Black-Scholes CPU, no FFI."

[lib]
name = "ngv_opx_core"

[dependencies]
bytemuck = { version = "1.14", features = ["derive"] }
libm = "0.2"
rayon = "1.10"
