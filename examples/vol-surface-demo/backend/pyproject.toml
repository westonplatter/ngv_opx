[project]
name = "vol-surface-demo-backend"
version = "0.1.0"
description = "FastAPI WebSocket server streaming synthetic options quotes for the vol surface demo."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "numpy>=1.26.0",
]

[tool.uv]
package = false
