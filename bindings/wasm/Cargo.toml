[package]
name = "ngv-opx-wasm"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"
description = "WebAssembly bindings for ngv-opx (Black-76 pricing + IV solver)."

[lib]
crate-type = ["cdylib"]

[dependencies]
ngv-opx-core = { path = "../../crates/core" }
wasm-bindgen = "0.2"
js-sys = "0.3"
