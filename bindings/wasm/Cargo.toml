[package]
name = "ngv-opx-wasm"
version = "0.1.2"
edition.workspace = true
license.workspace = true
description = "WebAssembly bindings for ngv-opx (Black-76 pricing + IV solver)."

[lib]
crate-type = ["cdylib"]

[dependencies]
ngv-opx-core = { path = "../../crates/core", version = "0.1.2" }
wasm-bindgen = "0.2"
js-sys = "0.3"
